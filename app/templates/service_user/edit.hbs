<h1>Service User</h1>

{{#if controllers.service_user.model.errors.messages}}
  {{#each controllers.service_user.model.errors.messages}}
    <div class="error">
      {{this}}
    </div>
  {{/each}}
{{/if}}

<table>
  <tbody>
    {{#each buffer}}
    <tr>
      <td>
        <strong>{{key}}:</strong>
      </td>
      <td>
        {{input value=value}}
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>

<button {{action 'save'}}>Update</button>

{{outlet}}
