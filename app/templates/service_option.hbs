<div class="well">
  <table class="table">
    <tbody>
      <tr>
        <td>Name</td>
        <td>{{name}}</td>
      </tr>
    </tbody>
  </table>

  {{#link-to 'clinics'}}
    <button class="btn btn-primary">
      <i class="icon-white icon-tasks"></i> New Appointment
    </button>
  {{/link-to}}
</div>

{{outlet}}
