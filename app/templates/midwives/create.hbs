<h1>Add Service Provider</h1>

{{#if controllers.midwife.model.errors.messages}}
  {{#each controllers.midwife.model.errors.messages}}
    <div class="error">
      {{this}}
    </div>
  {{/each}}
{{/if}}

{{#em-form model=model}}
  {{em-input property="name" label="Full Name" placeholder="Enter a name..."}}
  {{em-input property="login_name" label="Username" placeholder="Enter a username..."}}
  {{em-input property="password" label="Password" placeholder="And password..." type="password"}}
  {{em-input property="password" label="Password" placeholder="And password..." type="password"}}
  {{!-- {{em-text property="comment" label="Coment" placeholder="Comment please.." rows=4}} --}}
{{/em-form}}
